name = "mediapipe-text-classifier-for-web"
main = "web/dist"

[pages]
project_name = "mediapipe-text-classifier-for-web"
build_command = "npm install && npm run build"
build_output_dir = "web/dist"

[vars]
# 可在此处定义 Cloudflare Pages 运行时的环境变量（如 API Token 等）
# 例如：API_BASE_URL = "https://example.com"

